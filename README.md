# 文件搜索工具 (Photograh Search)

## 功能特点

- **多条件搜索**：支持按文件夹路径、创建时间范围、文件类型和大小范围进行搜索
- **历史记录管理**：自动记录搜索历史，支持查看、应用、删除和清空历史记录
- **搜索结果排序**：支持点击列标题对搜索结果进行正序/倒序排序
- **日志记录**：详细记录每一次搜索操作，便于后续分析
- **直观的用户界面**：采用Tkinter开发，界面简洁易用
- **响应式设计**：窗口大小可调整，组件自动适应

## 使用方法

1. **选择文件夹**：点击"浏览"按钮选择要搜索的文件夹
2. **设置搜索条件**：
   - 选择创建时间范围
   - 选择文件类型
   - 设置文件大小范围
3. **执行搜索**：点击"开始搜索"按钮
4. **查看结果**：在搜索结果表格中查看匹配的文件
5. **使用历史记录**：点击"历史记录"按钮查看和应用之前的搜索条件

## 支持的文件类型

| 文件类型 | 支持的格式 |
|---------|------------|
| 所有文件 | *.* |
| 所有图片 | *.jpg; *.jpeg; *.png; *.gif; *.bmp; *.tiff; *.tif |
| JPEG格式 | *.jpg; *.jpeg |
| PNG格式 | *.png |
| TIFF格式 | *.tiff; *.tif |
| RAW格式 | *.cr2; *.cr3; *.nef; *.arw; *.dng; *.rw2; *.orf; *.pef; *.srw; *.raf; *.mos |
| PSD格式 | *.psd |
| DNG格式 | *.dng |
| 视频文件 | *.mp4; *.avi; *.mkv; *.mov; *.wmv; *.m4v |
| 压缩文件 | *.zip; *.rar; *.7z; *.tar; *.gz |

## 安装和运行

### 方法一：直接运行EXE文件

1. 从发布包中获取`Photograh Search.exe`文件
2. 双击运行即可，无需安装Python环境

### 方法二：从源代码运行

1. 确保已安装Python 3.11或更高版本
2. 安装依赖包：
   ```
   pip install tkcalendar
   ```
3. 运行主程序：
   ```
   python File_Search_Tool.py
   ```

## 项目结构

```
Photograh Search/
├── File_Search_Tool.py        # 主程序文件
├── history_manager.py          # 历史记录管理模块
├── log_interpreter.py         # 日志解释程序
├── search_history.json        # 搜索历史存储文件
├── search_logs/               # 搜索日志文件夹
├── log_abbreviations.md       # 日志缩写说明文档
└── README.md                  # 项目说明文档
```

## 日志功能

- 每次搜索操作都会生成一个日志文件，保存在`search_logs`文件夹中
- 日志文件使用英文缩写记录，以节省空间
- 日志文件包含搜索条件、搜索结果数量、搜索耗时等信息
- 可以使用`log_interpreter.py`工具解析和查看日志内容

## 历史记录功能

- 自动保存搜索条件到历史记录
- 支持双击历史记录直接应用到当前搜索
- 支持删除单个历史记录或清空所有历史记录
- 历史记录保存在`search_history.json`文件中

## 版本记录

### 1.0.0 beta
- 初始版本
- 支持多条件文件搜索
- 支持搜索历史记录管理
- 支持搜索结果排序
- 支持日志记录和解析
- 支持EXE打包

## 作者信息

作者：埃德蒙唐泰斯

## 注意事项

1. 建议不要在系统目录（如C:\Windows）中进行深度搜索，可能会导致搜索速度较慢
2. 搜索大量文件时，建议缩小搜索范围或增加筛选条件
3. 日志文件会占用一定磁盘空间，定期清理`search_logs`文件夹可以释放空间
4. 历史记录文件建议不要手动编辑，以免格式错误导致程序异常

## 许可证

本项目采用MIT许可证，可自由使用、修改和分发。